# Outputs

Generated figures, tables, and reports belong under `outputs/figures/`, `outputs/tables/`, and `outputs/reports/`. These products are ignored by Git by default because they are reproducible from the code and local data. Historical output files are not dependencies of the new pipeline.
